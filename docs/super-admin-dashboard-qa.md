# צ׳ק ליסט בדיקה — סופר אדמין (Dashboard מלא)

מסמך לבדיקה **ידנית** לאחר התחברות כ־**`super_admin`**.  
מטרה: לוודא שכל אזורי ה־Admin נטענים, הפעולות נשמרות למסד, וההתנהגות עקבית.

**ערוץ Admin (ייצור):** `https://hotel-management-rho-two.vercel.app`  
**כניסה:** `/login`

---

## לפני שמתחילים

| פריט | פעולה |
|------|--------|
| משתמש | חשבון עם `role = super_admin` בטבלת `users` + אותו משתמש ב־Supabase Auth |
| עוגיה | התחברות דרך הדפדפן — נשמרת עוגיית `sb-access-token` (אימות הפאנל) |
| `manage-user` | אם אתם יוצרים/מעדכנים משתמשים מהפאנל — לפרוס את ה־Edge Function המעודכן בסופבייס |
| בנייה קודית | מתוך `admin/`: `npx tsc --noEmit` · `npm run lint` · (אופציונלי) `npm run build` |

בדיקה אוטומטית אחרונה במאגר: **TypeScript + ESLint עברו בהצלחה** על קוד ה־Admin.

---

## מה הסנכרון אומר בתרחיש הזה?

- הנתונים ב־**Dashboard** נטענים בעיקר מה־**שרת Next.js** שמשתמש ב־**Service Role ל־Supabase** (`supabaseAdmin`) — לא מסתמכים על RLS של הלקוח.
- רוב העמודים **לא פותחים WebSocket מתמשך** מהדפדפן: לאחר פעולה באפליקציית צווט / PWA כדאי **לרענן את העמוד** ב־Admin כדי לראות עדכון מיידי.
- **אוטומציות**, **משתמשים (דרך API)** וכו׳ מתבססים על **עוגיות** — אין לפתוח את הטאב ב־`/api/...` בטרמינל גולמי בלי העתקת cookies (הבקשות נטענות מתוך הדפדפן המחובר).

---

## סדר מומלץ לבדיקה (כל המסך)

סמנו ✓ בהתאם למה שראיתם בעיניים.

> ✅ **הצ'קליסט הזה בוצע בפועל** (לא רק תיאורטית) ב-2026-08-30, מול הסביבה החיה, עם משתמש super_admin אמיתי ומשתמש CEO זמני שנוצר ונמחק לצורך בדיקת ההרשאות. תוצאות וממצאים מסומנים בכל סעיף.

### 1. כניסה ותפריט
- [x] `/login` — התחברות מצליחה, מועבר ל־`/dashboard`
- [x] בסרגל: **כל** הפריטים מופיעים, ועוד 2 שלא היו ברשימה המקורית: **רישיונות** ו**הזמנות אורחים** (`amenity_orders`)
- [x] מתג שפה קיים (עב/EN) — לא נבדק מעבר בפועל

### 2. סקירה — `/dashboard`
- [x] מספרים אמיתיים ומתעדכנים: 4 מלונות, 9-10 משתמשים (השתנה בזמן הבדיקה כשיצרתי/מחקתי משתמש בדיקה), 0 קריאות פתוחות ברמת מערכת
- [x] ניווט בין עמודי הסרגל תקין

### 3. מלונות — `/dashboard/hotels`
- [x] טבלה נטענת עם 4 המלונות האמיתיים (שם, תוכנית, סטטוס, ערכת עיצוב, תאריך)
- [ ] **ThemePicker** — לא נבדק בפועל (לא לחצתי לשנות ערכת עיצוב על המלון האמיתי)
- [x] **New Hotel** — `/dashboard/hotels/new` נגיש לסופר; **נבדק גם שחסום** למשתמש שאינו סופר (מפנה חזרה ל-overview)
- [x] **עריכת מלון** — נבדק על מלון רוקסון האמיתי: כל הקישורים (חדרים, שירותים נוספים, **כרטיסיות בקשה** ✨, QR, פוסטר) נפתחים
  - [x] **כרטיסיות בקשה** (הפיצ'ר החדש) — **נבדק מקצה לקצה**: הכרטיסיות נטענות, טוגל אחד נבדק בפועל (נכתב ל-DB, אומת ישירות, והוחזר למצב המקורי)
  - [ ] מחיקת מלון — לא נבדק (מסוכן על נתונים אמיתיים), אבל **נבדק שהכפתור מוסתר** למשתמש שאינו סופר

### 4. חדרים — `.../hotels/[id]/rooms`
- [x] רשימת 168 חדרים (7 קומות, דילוג על מספר 13 בכל קומה) נטענת נכון, כפתורי הוספה/עריכה/מחיקה קיימים
- [ ] בולק-אד לא הופעל בפועל (חדרים כבר קיימים)

### 5. QR / פוסטר
- [x] דף QR — QR ללובי + 168 QR לחדרים, הורדת PNG בודדת/ZIP, קישורים נכונים למלון האמיתי
- [x] פוסטר A4 — נטען עם לוגו, שם מלון, וברכה ב-4 שפות

### 6. משתמשים — `/dashboard/users`
- [x] רשימה + סינונים (מלון, תפקיד) — 9 משתמשים אמיתיים בקבוצות לפי מלון
- [x] **הוסף משתמש** — נבדק בפועל: נוצר משתמש CEO זמני (`qa-temp-regression-test@hotel.com`) בהצלחה
- [x] **עריכה + מחיקה** — נבדק בפועל על אותו משתמש: נמחק בסוף הבדיקה, כולל אימות ישיר ב-DB ש-`auth.users` נוקה ולא נשאר orphaned

### 7. אנליטיקה — `/dashboard/analytics`
- [x] Global Analytics מציג 3 מלונות עם נתונים אמיתיים (Hotel Alpha 9, רוקסון 3, דן ת"א 1) — התיקון הקודם (שילוב tickets+guest_requests) עדיין עובד

### 8. Audit logs — `/dashboard/logs`
- [x] הגישה **נחסמת** בפועל למשתמש שאינו סופר (מפנה ל-overview)
- ⚠️ **ממצא**: העמוד מציג **0 שורות** למרות פעילות אמיתית בסשן. הסיבה: השאילתה קוראת מטבלת `ticket_updates` (עדכוני קריאות אחזקה), **לא** לוג פעולות אדמין כללי כמו שהשם "Audit Logs" מרמז. ריק כי אין עדיין עדכוני tickets בנתוני הבדיקה — לא באג, אבל השם מטעה. שווה לשקול rename לעתיד.

### 9. צ׳קליסטים — `/dashboard/checklists`
- [x] נטען עם 3 תבניות אמיתיות (ביקורת אחזקה, ניקיון VIP, ניקיון רגיל)
- [ ] יצירה/עריכה לא נבדקה בפועל
- [x] **נבדק שחסום** למשתמש שאינו סופר (מפנה ל-overview)

### 10. אוטומציות — `/dashboard/automations`
- [x] נטען, מציג מצב ריק נכון ("אין אוטומציות, צור אחת!") — תואם ל-`scheduled_tasks` ריק שאומת גם דרך ה-Edge Function
- [ ] יצירה/עריכה/מחיקה לא נבדקו בפועל

### 11. בקשות אורחים — `/dashboard/guest-requests`
- [x] טבלה עם 13 בקשות אמיתיות מכל 4 המלונות, סינונים (מלון/סטטוס/תאריך) קיימים, dropdown שינוי סטטוס inline קיים בכל שורה
- [ ] שינוי סטטוס בפועל לא נבדק (לא רציתי לגעת בבקשות אמיתיות)

### 12. משוב אורחים — `/dashboard/guest-feedback`
- [x] נטען, מצב ריק נכון ("אין משובים") — סינון מלון קיים

### 13. התנתקות
- [x] `/api/logout` עובד — נבדק **3 פעמים** בפועל במהלך הבדיקה (מעבר בין super_admin למשתמש הבדיקה וחזרה)

---

## בדיקת “מה לא אמור לקרות” (רגרסיה הרשאות)

✅ **בוצעה בפועל** מול משתמש CEO אמיתי (`qa-temp-regression-test@hotel.com`, מלון Hotel Alpha, נוצר ונמחק לצורך הבדיקה):

- [x] **אין** גישה ל־`/dashboard/logs` — מפנה ל-`/dashboard` (overview מצומצם למלון)
- [x] **אין** גישה ל־`/dashboard/checklists` — מפנה ל-`/dashboard`
- [x] `/dashboard/hotels` מפנה נכון לעריכת המלון של המשתמש בלבד (Hotel Alpha) — לא לרשימה הגלובלית
- [x] `/dashboard/hotels/new` חסום — מפנה ל-`/dashboard`
- [x] **בונוס**: נבדק גם ישירות מול קוד הפיצ'ר שנבנה הערב — `/dashboard/hotels/<id>/request-tiles` מחזיר **404 אמיתי** (לא רק מוסתר בממשק) למשתמש שאינו סופר, כולל הכפתור עצמו שנעלם נכון מעמוד עריכת המלון

---

## רישום בעיות

| תסמין | מה לבדוק |
|---------|-----------|
| 401 בעת שמירה | עוגיה `sb-access-token` / תוקף הסשן |
| 403 ב־`/api/users` | Edge `manage-user` לא מעודכן או הרשאות caller |
| ללא נתונים בטבלאות | פרויקט Supabase לא נכון ב־`.env.local` של Vercel |
| שינוי Theme לא נשמר | Network ל־`PATCH .../api/hotels/[id]` ותשובת 200 |

---

קובץ נלווה בתשתית: [`docs/התחברות-ומסכים-לפי-תפקיד.md`](./התחברות-ומסכים-לפי-תפקיד.md) — מאיפה כל תפקיד נכנס.
